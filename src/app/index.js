import React, { PropTypes } from 'react';

const Copyist = ({ markdown }) => (
  <div class="copyist">
    stuff goes here
  </div>
);

Copyist.propTypes = {
  markdown: PropTypes.string
};

export default Copyist;
